return {
  client = {
    keys = require("keys.client.clientkeys"),
    mouse = require("keys.client.clientmouse"),
  },
  global = {
    keys = require("keys.client.globalkeys"),
    mouse = require("keys.client.globalmouse"),
  },
  modkey = require("keys.modkey"),
}
